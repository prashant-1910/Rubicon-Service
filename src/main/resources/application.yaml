spring:
  application:
    name: rubicon
  datasource:
    url: jdbc:postgresql://localhost:5432/rubicon
#    username: postgres
#    password: 1916
    username: rub_user
    password: rubicon1916
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 9090

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
